This is a front-end Next.js app that is an agent builder platform. I am working on the voice part of the app. Currently creating a new route "/voice-new and integrating LiveKit components to it.

This is the voice part of this app will talk to a voice back-end. There is a reference repository that should not be modified and only taken reference from which is in the frontend-livekit-reference folder (don't copy the style only how it works).

/voice-agent-builder is the older route we will copy and modify for livekit.

Keep the code clean and modular.
