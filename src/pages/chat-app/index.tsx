import { Layout } from "@/components/custom/layout";

export default function ChatApp() {
  return (
    <Layout fixed>
      <div>{/* <ChatBox agent={{}} setChatData={() => { }} /> */}</div>
    </Layout>
  );
}
